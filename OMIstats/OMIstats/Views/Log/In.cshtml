@model OMIstats.Models.Usuario

@{
    ViewBag.Title = "Iniciar sesión";
}

<h1 class="titulo">Iniciar sesión</h1>

@if (ViewBag.GUIDerror)
{
    <p class="textoGrande">La dirección que usaste ya no es válida, intenta @Html.ActionLink("iniciando sesión", "Index", "Log") otra vez.</p>
}

@if (ViewBag.NoMatch)
{
    <div class="textoGrande">
        <p class="errorTextoRojo">No se encontró un usuario en el sistema que corresponda con la cuenta con la que iniciaste sesión.</p>
    
        <p>
            <b>Si estás participando este año</b>, y estás buscando tus resultados,
            lo puedes encontrar en
            <a href="http://www.olimpiadadeinformatica.org.mx/OMI/Perfil/PaginaPersonal.aspx">tu página personal</a>.
            Esta página es solo para agregar tus datos al histórico de olimpiadas nacionales
            presenciales pasadas.
        </p>

        <p class="errorTextoRojo"><b>ESTA NO ES LA PÁGINA PARA PRESENTAR EL EXAMEN EN LÍNEA</b></p>

        <p>Si estás seguro que has participado en una <b>olimpiada nacional</b> como competidor, líder, delegado, asesor, invitado, 
            comité o con algún otro rol (sin importar si fue en primaria, secundaria o preparatoria), puedes hacer lo siguiente:</p>
    
        <ol>
            <li>
                 @Html.ActionLink("Búscate","Index","Buscar", null, new { target="_blank"}) en nuestra base de datos y después:
                <ul>
                    <li class="espaciado">
                        Si no te encuentras,
                        @Html.ActionLink("danos tus datos","LogIn","Request", new {
                            nombre = Model.Nombre, correo = Model.Email, tipo = OMIstats.Models.Peticion.TipoPeticion.NO_ESTOY }, null)
                        y nosotros te agregamos.
                    </li>
                </ul>
            </li>
            <li class="espaciado">
                Si sí te encuentras, verifica que los datos en las tablas de resultados
                (o en la lista de la delegación) sean <b>los mismos</b>
                a los que tienes en tu perfil. Basta con que tu nombre o tu correo sean iguales, pero
                tienen que estar deletreados de la misma forma.
                <ul>
                    <li class="espaciado">
                        Si no hay ningún error,
                         @Html.ActionLink("avísanos","LogIn","Request", new {
                            nombre = Model.Nombre, correo = Model.Email, tipo = OMIstats.Models.Peticion.TipoPeticion.NO_ERROR }, null)
                        para revisar qué es lo que está mal con tus datos.
                    </li>
                    <li class="espaciado">
                        Si hay un error en las tablas de resultados o delegación,
                         @Html.ActionLink("háznoslo saber","LogIn","Request", new {
                            nombre = Model.Nombre, correo = Model.Email, tipo = OMIstats.Models.Peticion.TipoPeticion.ERROR }, null)
                        y los actualizamos.
                    </li>
                </ul>
            </li>
        </ol>
    </div>
}
